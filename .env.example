# Database
DATABASE_URL="postgresql://user:password@localhost:5432/growtools"

# Authentication
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Encryption
# Generate with: openssl rand -hex 32
COOKIE_ENCRYPTION_KEY="your-32-character-secret-key-here"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_EXTENSION_ID="chrome-extension-id-here"
NODE_ENV="development"

# Email Configuration
# Choose provider: "resend" or "smtp"
EMAIL_PROVIDER="resend"

# Resend Configuration (Recommended for Vercel)
# Get API key from https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxxxxx"
EMAIL_FROM="noreply@alidigitalsolution.in"
EMAIL_FROM_NAME="AliDigitalSolution"

# SMTP Configuration (Alternative to Resend)
# Uncomment and configure if using SMTP instead of Resend
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# Google OAuth (for NextAuth)
# Get credentials from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Firebase Configuration (Optional - for client-side Firebase auth)
# Get from https://console.firebase.google.com/
# Note: Default values are already configured in firebase-auth.ts
# You can override them here if needed
NEXT_PUBLIC_FIREBASE_API_KEY="AIzaSyCf7r76hx5_9mjKaN8lZNrJf1OnWdlNGAk"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="alitool-22bc4.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="alitool-22bc4"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="alitool-22bc4.firebasestorage.app"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="271496020101"
NEXT_PUBLIC_FIREBASE_APP_ID="1:271496020101:web:69e21bd5f88bb864b7a9ec"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-GGSQ1G3QQT"

# Cron Job Secret (for subscription validation endpoint)
# Generate with: openssl rand -hex 32
CRON_SECRET="your-secret-key-for-cron-jobs"

# Payment Gateway (Paygic)
PAYGIC_MERCHANT_ID="your-merchant-id"
PAYGIC_PASSWORD="your-password"

# Google Analytics
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
