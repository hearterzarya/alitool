# =============================================================================
# AliDigitalSolution / alitool — Environment Variables
# Copy to .env.local and fill in values. Do NOT commit .env or .env.local.
# =============================================================================

# -----------------------------------------------------------------------------
# Database (required)
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# -----------------------------------------------------------------------------
# NextAuth (required)
# -----------------------------------------------------------------------------
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
# Local: http://localhost:3000  |  Production: https://alidigitalsolution.in
NEXTAUTH_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# App URL (recommended for emails, password reset, etc.)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# Google OAuth (required for Google login)
# Get from https://console.cloud.google.com/apis/credentials
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"

# -----------------------------------------------------------------------------
# Email (required for OTP, order confirmations)
# -----------------------------------------------------------------------------
# Provider: "resend" or "smtp"
EMAIL_PROVIDER="resend"
EMAIL_FROM="noreply@alidigitalsolution.in"
EMAIL_FROM_NAME="AliDigitalSolution"

# Resend (recommended) — https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxxxxx"

# SMTP (alternative) — uncomment if using SMTP instead of Resend
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# -----------------------------------------------------------------------------
# WhatsApp (optional — or set in Admin → Settings)
# -----------------------------------------------------------------------------
WHATSAPP_NUMBER="919155313223"
WHATSAPP_DEFAULT_MESSAGE="Hello! I need help with my subscription."

# -----------------------------------------------------------------------------
# Payment — Paygic (optional, for checkout)
# -----------------------------------------------------------------------------
PAYGIC_MERCHANT_ID="your-merchant-id"
PAYGIC_PASSWORD="your-password"

# -----------------------------------------------------------------------------
# Cron (optional — for /api/subscriptions/validate)
# Generate: openssl rand -hex 32
# -----------------------------------------------------------------------------
CRON_SECRET="your-cron-secret"

# -----------------------------------------------------------------------------
# Encryption (optional — cookie encryption; default used if not set)
# Generate: openssl rand -hex 32
# -----------------------------------------------------------------------------
COOKIE_ENCRYPTION_KEY="your-32-char-hex-key"

# -----------------------------------------------------------------------------
# Firebase (optional — app has defaults for alitool-a5847 in code)
# Override here or in Vercel for a different project.
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_FIREBASE_API_KEY=""
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="alitool-a5847.firebaseapp.com"
# NEXT_PUBLIC_FIREBASE_PROJECT_ID="alitool-a5847"
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="alitool-a5847.firebasestorage.app"
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="173949694300"
# NEXT_PUBLIC_FIREBASE_APP_ID="1:173949694300:web:e60b17d4d1c3b852101199"
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-8JLJVTG8CF"

# -----------------------------------------------------------------------------
# Analytics & storage (optional)
# -----------------------------------------------------------------------------
# Google Analytics
NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# Vercel Blob (optional — for bundle/tool icon uploads; else files go to public/)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxx"

# -----------------------------------------------------------------------------
# Extension (optional)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_EXTENSION_ID="chrome-extension-id-here"
